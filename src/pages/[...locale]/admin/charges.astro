---
import Layout from "../../../layouts/Layout.astro";
import App from "../../../components/App.svelte";
const { t, lang, session } = Astro.locals;
import Contributions from "../../../components/Admin/Contributions.svelte";
---

<Layout title={t("pages.admin.charges.title")} description={t("pages.metadata.description")}>
    <App client:load locale={lang} session={session}>
        <div class="wrapper">
            <div class="flex w-full flex-col gap-10 py-10">
                <div class="flex flex-col gap-4">
                    <h2 class="text-[2.5rem]/12 font-bold tracking-[0%] text-black">
                        {t("pages.admin.charges.title")}
                    </h2>
                    <p class="text-content">{t("pages.admin.charges.description")}</p>
                </div>

                <Contributions client:load />
            </div>
        </div>
    </App>
</Layout>
